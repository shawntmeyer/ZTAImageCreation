{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_generator": {
      "name": "bicep",
      "version": "0.21.1.54444",
      "templateHash": "16689163161444589334"
    }
  },
  "parameters": {
    "location": {
      "type": "string",
      "defaultValue": "[resourceGroup().location]"
    }
  },
  "variables": {
    "imageName": "TestImage",
    "versionName": "1.1.1"
  },
  "resources": [
    {
      "type": "Microsoft.Compute/galleries/images/versions",
      "apiVersion": "2022-01-03",
      "name": "[format('{0}/{1}/{2}', split(variables('imageName'), '/')[0], split(variables('imageName'), '/')[1], variables('versionName'))]",
      "location": "[parameters('location')]"
    }
  ],
  "outputs": {
    "versionId": {
      "type": "string",
      "value": "[resourceId('Microsoft.Compute/galleries/images/versions', split(variables('imageName'), '/')[0], split(variables('imageName'), '/')[1], variables('versionName'))]"
    }
  }
}